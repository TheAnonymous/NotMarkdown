{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base window capability without filesystem, shell, or network plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

